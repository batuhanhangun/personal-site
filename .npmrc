# Pin exact versions to avoid surprise upgrades from ^ / ~ ranges
save-exact=true

# Fail `npm install` if the running Node.js version doesn't satisfy
# the "engines" field in package.json (>=22.12.0)
engine-strict=true
